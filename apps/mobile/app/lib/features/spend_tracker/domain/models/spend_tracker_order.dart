enum SpendTrackerOrderStrategy {
  // The user orders the trackers manually via drag & drop
  manual,
  // The trackers are ordered alphabetically by name
  alphabetical,
  // The trackers are ordered based on id
  created,
}
